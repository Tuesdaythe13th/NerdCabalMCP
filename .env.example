# NerdCabalMCP — Environment Variables
# Copy this file to .env and fill in your values.
# .env is git-ignored and should NEVER be committed.
#
# Most agents work without any keys — they generate notebooks/code locally.
# Keys below unlock specific integrations (repo cloning, model APIs, etc.).

# ── Core / Paper2Agent Pipeline ────────────────────────────────────────────
# Required by create_paper_agent when accessing private GitHub repos.
# Get a fine-grained token at: https://github.com/settings/tokens
# Minimum scope: "Contents" read access on the target repo.
GITHUB_TOKEN=

# Passed to the Paper2Agent.sh pipeline when --api is specified.
# This is the Anthropic API key used inside spawned sub-agents.
ANTHROPIC_API_KEY=

# ── Optional LLM Providers ─────────────────────────────────────────────────
# Only needed if generated notebooks will call these APIs.
OPENAI_API_KEY=
GOOGLE_API_KEY=
HUGGINGFACE_TOKEN=

# ── MLflow (generate_mlflow_query_plan) ────────────────────────────────────
# Point to your MLflow tracking server if you have one.
MLFLOW_TRACKING_URI=http://localhost:5000
# MLFLOW_TRACKING_USERNAME=
# MLFLOW_TRACKING_PASSWORD=

# ── FiftyOne (visualize_dataset) ───────────────────────────────────────────
# Leave blank to use the default local MongoDB.
# FIFTYONE_DATABASE_URI=mongodb://localhost:27017

# ── Logging ────────────────────────────────────────────────────────────────
# Set to "debug" for verbose MCP server output.
LOG_LEVEL=info
